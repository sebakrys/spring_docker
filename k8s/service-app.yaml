apiVersion: v1
kind: Service
metadata:
  name: aplikacja
spec:
  selector:
    app: aplikacja
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer # Lub NodePort dla dostępu zewnętrznego